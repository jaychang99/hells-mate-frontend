import styled from "@emotion/styled";

export const GrayLt = styled.div`
  width: 56px;
  height: 56px;
  border-radius: 50%;
  color: ${({theme}) => theme.color.black500};
  text-align: center;
  line-height: 48px;
  font-size: 44px;
  cursor: pointer;
  float: left;
`;